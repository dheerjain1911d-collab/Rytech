# Multi-Model Support TODO

- [x] 1. Update `model.py` to train and save GradientBoosting, NaiveBayes, DecisionTree, KNN
- [x] 2. Update `app.py` to load all models and accept `model` param in `/predict`
- [x] 3. Update `templates/predict.html` to add algorithm selector dropdown
- [x] 4. Update `static/script.js` to send selected algorithm to backend
- [x] 5. Run `model.py` to generate new `.pkl` files
- [x] 6. Test server and verify predictions with each algorithm

