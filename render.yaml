services:
  - type: web
    name: rytech
    env: python
    runtime: python-3.11
    buildCommand: pip install -r requirements.txt
    startCommand: exec gunicorn app:app --workers 1 --timeout 120 --bind 0.0.0.0:$PORT
